import mongoose from "mongoose"
let { model, Schema } = mongoose




/*let reciepe = new Schema({
    id:String,
    name:String,
    img:String,
    ing:Array,
    type:String,
    price:Number,
    quantity:{type:Number,default:1},
    rate:{type:Number,default:1}
})

export default  mongoose.model("datas",reciepe)*/


